import { AccessResourceDirective } from './access-resource.directive';

describe('AccessResourceDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessResourceDirective();
    expect(directive).toBeTruthy();
  });
});
